import './spinner.css';
const Spinner = (): JSX.Element => {
    return (
        <div className="wrap-spinner">
            <span className="loader"></span>
        </div>
    );
};

export default Spinner;
